# Docker

Current local Docker Compose is at the repository root: `docker-compose.yml`.

Docker Desktop is installed on the local machine, but WSL/Virtual Machine Platform needs admin elevation before Linux containers can run.

